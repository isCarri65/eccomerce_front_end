import { useState } from "react";
import { IProduct } from "../../../types/Product/IProduct";

export const ProductCatalogScreen = () => {
  const [PopulatesProduct, setPopulatesProduct] = useState<IProduct[]>([]);
  return (
    <div>
      <div></div>
    </div>
  );
};
